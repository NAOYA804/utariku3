import React from 'react';
import ReactDOM from 'react-dom/client';
import SongRequestApp from './SongRequestApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SongRequestApp />
  </React.StrictMode>
);
